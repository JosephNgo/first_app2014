<%= form_for(@crew_class) do |f| %>
  <% if @crew_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crew_class.errors.count, "error") %> prohibited this crew_class from being saved:</h2>

      <ul>
      <% @crew_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.number_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :class_desc %><br>
    <%= f.text_field :class_desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
